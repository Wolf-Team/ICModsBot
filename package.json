{
  "name": "icmodsbot",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "out/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node --experimental-modules -r source-map-support/register out/index.js -d",
    "buildstart": "tsc -p tsconfig.json & cls & npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "WolfTeam",
  "license": "MIT",
  "dependencies": {
    "icmodsapi": "^1.0.0",
    "nodevk-ts": "^0.2.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^15.14.2",
    "source-map-support": "^0.5.20"
  }
}
